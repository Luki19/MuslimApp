<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="koding.web.muslimapp">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/w"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/w"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DoaSetelahBelajar"></activity>
        <activity android:name=".DoaSebelumBelajar" />
        <activity android:name=".DoaKeluarMesjid" />
        <activity android:name=".DoaMasukMesjid" />
        <activity android:name=".DoaKeluarKamarMandi" />
        <activity android:name=".DoaMasukKamarMandi" />
        <activity android:name=".DoaMasukRumah" />
        <activity android:name=".DoaKeluarRumah" />
        <activity android:name=".MainDoa" />
        <activity android:name=".MainHalal" />
        <activity android:name=".SholatTest" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>